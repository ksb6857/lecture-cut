# 피드백 원장

받은 지적과 스스로 찾은 결함을 규칙으로 바꾼 기록이다. **막는 곳**이 비어 있으면 아직
사람 주의에 기대고 있다는 뜻이다. 그런 줄은 도구로 옮길 때까지 남겨 둔다.

| 날짜 | 무엇이 틀렸나 | 까닭 | 규칙 | 막는 곳 |
|---|---|---|---|---|
| 2026-08-08 | 편집 중이던 캡컷 프로젝트가 날아갔다 | 캡컷이 켜진 채 드래프트 폴더를 고쳤다 | 캡컷이 떠 있으면 드래프트를 건드리지 않는다 | 드래프트를 고치는 도구마다 `capcut_running` 으로 멈춤 |
| 2026-08-13 | 드래프트가 하루 동안 무음이었다 | JSON 만 보면 멀쩡해 보인다 | 소리를 확인한 뒤에 넘긴다 | `build_capcut_draft` 끝의 `verify_draft_audio` |
| 2026-08-17 | 강조 상자가 표 밖으로 삐져나왔다 | 배율을 정지 프레임에 맞춰 순환논리로 정했다 | 캡컷 렌더에서 잰다. K=2.64 | `apply_highlights.K` 주석 |
| 2026-08-17·24 | 이름을 바꾸다 소재 경로가 끊겼다 | 캡컷은 폴더 이름을 바꿔도 안의 절대경로를 안 고친다 | 판은 목록 밖 스냅샷으로. 폴더 이름으로 찾지 않는다 | `draft_snapshot` · `draft_doctor 진단` |
| 2026-09-08 | 사용자가 손본 발표 자료 일곱 군데가 사라졌다 | 편집본을 참고만 하고 생성 스크립트로 새로 만들었다 | 사용자가 손본 파일이 원본이다. 요청한 곳만 고치고 차이 0을 잰다 | 발표 자료 비교 도구(차이 0) |
| 2026-09-20 | 말 꼬임이 러프컷에 그대로 남았다 | 흘린 낱말을 안 메웠고, 계획기가 못 자른 지시를 안 쟀다 | 메우기는 필수. 삭제 잔여를 잰다 | `final_check` 삭제 잔여 · SKILL 3.5단계 |
| 2026-09-23 | 쉼 규칙을 안 따랐다 | 한국어 전사에 문장부호가 적어 모든 컷을 문장 끝으로 봤다 | 종결어미로 문장 끝을 가린다 | `cut_planner` 가 문장 중간 0곳이면 멈춤 · `final_check` 쉼 |
| 2026-09-23 | 말하다 중간에 잘렸다 | 낱말 시각(±0.3~1초)으로 덩어리 한가운데를 잘랐다 | 덩어리는 진짜 무음에서만 나눈다 | `refine_speech` · `final_check` 소리 위 컷 0 |
| 2026-09-23 | 판정을 고쳤는데 되돌아갔다 | 충돌 해소기가 겹치는 지시를 넓게 합쳤다 | 사람이 고친 지시는 exact | `resolve_delete_conflicts` 가 exact 를 건너뜀 |
| 2026-09-23 | 검산을 통과했는데 헛시작이 남았다 | 검산기는 같은 낱말·같은 문장만 본다 | 들리는 대본을 끝까지 읽는다 | 사람 주의 (`final_check` 같은 문장 검사로 일부) |
| 2026-09-23 | 한 화면에 오래 멈춰 지루하다 | 녹화에 교안 애니메이션이 없었다 | 교안에 나타내기를 넣고 영상에는 굽는다 | `ppt_anim` · `bake_builds` |
| 2026-09-23 | 어디를 말하는지 모르겠다 | 강조 상자가 없었다 | 교안 항목과 실습 화면에 상자를 건다 | `apply_highlights` 계획 |
| 2026-09-23 | 장면이 바뀔 때 전환이 없다 | 사람이 손으로 넣던 것을 자동화하지 않았다 | 넣는 곳 규칙대로 참조 드래프트에서 복제 | `apply_transitions` |
| 2026-09-24 | 나오기 전 카드 자리에 흐린 테두리가 비쳤다(스스로 찾음) | 색 맞춤을 묶음마다 달리 했고, 섞은 값을 버림했다 | 한 값으로 맞추고 반올림, 숨길 자리 +12px | `compose_builds` · 검사 두 줄([효과.md](효과.md)) |
| 2026-09-24 | 나오기 전 상자 윗부분이 보였다(스스로 찾음) | 이미 나온 글상자 틀이 아래 상자를 덮었다 | 숨길 도형이 그려지는 곳은 틀 안이어도 숨긴다 | `compose_builds` |
| 2026-09-24 | 강조 상자 네 개가 엉뚱한 곳에 붙었다(스스로 찾음) | 여러 칸으로 모은 그림의 칸 안 좌표를 두 배 안 했다 | 상자마다 확인 그림을 본다 | 사람 주의 (확인 그림) |
