pycapcut==0.0.3
silero-vad>=5.1
torch>=2.0
numpy>=1.24
faster-whisper>=1.2
transformers>=4.40
soundfile>=0.12
requests>=2.31   # ElevenLabs Scribe 전사(transcribe_scribe)를 쓸 때
# 11단계 효과에만 필요하다
python-pptx>=0.6.21
lxml>=4.9
Pillow>=10.0
opencv-python>=4.8
pywin32>=306; sys_platform == "win32"   # 교안을 파워포인트로 렌더할 때(export_slides·render_builds)
